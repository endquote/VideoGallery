{
  "version": "0.2.0",
  "configurations": [{
    "type": "node",
    "request": "launch",
    "name": "server - mac",
    "program": "${workspaceRoot}/server/main.js",
    "cwd": "${workspaceRoot}/server",
    "env": {
      "NODE_CONFIG_DIR": "${env:HOME}/rgbtv"
    }
  }, {
    "type": "node",
    "request": "launch",
    "name": "server - win",
    "program": "${workspaceRoot}/server/main.js",
    "cwd": "${workspaceRoot}/server",
    "env": {
      "NODE_CONFIG_DIR": "C:\\rgbtv"
    }
  }]
}
